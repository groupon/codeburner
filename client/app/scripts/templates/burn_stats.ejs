<div class="container-fluid">
  <div class="row">
    <div class="col-lg-12">
      <center><h3><%= stats['burns'].toLocaleString('en') %> <small>revisions of</small> <%= stats['services'].toLocaleString('en') %> <small>services scanned</small></h3><center>
    </div>
  </div>

  <div class="row">
    <div class="col-lg-12">
      <center><h3><%= stats['lines'].toLocaleString('en') %> <small>lines burned in</small> <%= stats['files'].toLocaleString('en') %> <small>files</small></h3><center>
    </div>
  </div>

  <div class="row">
    <div class="col-lg-12">
      <center><h3><%= stats['total_findings'].toLocaleString('en') %> <small> possible vulnerabilities found</small></h3><center>
    </div>
  </div>
</div>

